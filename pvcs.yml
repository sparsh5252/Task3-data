apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: php-pvc
  labels: 
    app: php
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: html-pvc
  labels: 
    app: html
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi